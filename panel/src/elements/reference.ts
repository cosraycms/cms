// Defines <cosray-reference>: the node reference picker. A search box
// queries the panel's reference endpoint and picked nodes stack as
// removable rows. Language-neutral — the stored value is a zxx uid list.
import './reference/ReferenceElement.svelte';
